import tkinter as tk

root = tk.Tk() # creates a window program that automatically appears and then disappears. 
# Root -> variable name of the window Tk() -> class in tkinter class that initialize the GUI System and then return a window object

root.title("First Input Window")
root.mainloop()